# Changelog

## [Unreleased]

- Added the baseline-bound `lazycode.Workspace` planning model and deterministic
  create, update, and delete changesets.
- Added bounded Go, TOML, JSON, and JavaScript editing packages for installers,
  upgrades, and generators.
